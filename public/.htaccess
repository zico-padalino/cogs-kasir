<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    # Halaman sibuk statis — jangan lempar ke index.php (penting saat EP penuh).
    RewriteRule ^503\.html$ - [L]

    # Handle Authorization Header
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Handle X-XSRF-Token Header
    RewriteCond %{HTTP:x-xsrf-token} .
    RewriteRule .* - [E=HTTP_X_XSRF_TOKEN:%{HTTP:X-XSRF-Token}]

    # Redirect Trailing Slashes If Not A Folder...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # Send Requests To Front Controller...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>

# Saat proses PHP penuh, Apache/LiteSpeed harus tampilkan HTML statis (bukan PHP).
ErrorDocument 503 /503.html
ErrorDocument 429 /503.html
ErrorDocument 508 /503.html

# DomaiNesia PageSpeed/Speed Booster sering menaikkan Number of Processes → 503.
<IfModule mod_pagespeed.c>
    ModPagespeed off
</IfModule>
<IfModule pagespeed_module>
    ModPagespeed off
</IfModule>
